@model Nalanda.SMS.Areas.Admin.Models.ExtraActivityVM

@{
    ViewBag.Title = "Extra Activity Details";
}

<div>
    <hr />
    @using (Html.BeginForm("Delete", "ExtraActivity"))
    {
        @Html.HiddenFor(model => model.Id)
        @Html.HiddenFor(model => model.RowVersion)
        @Html.AntiForgeryToken()
        <p>
            <div class="btn-toolbar">
                @Html.ActionLink("Edit", "Edit", new { id = Model.Id }, new { @class = "btn btn-primary", style = "min-width:75px" })
                @Html.ConfirmSubmitButton("Delete", "Are you sure you want to delete?", "Confirm class deletion",
                 "<span class='glyphicon glyphicon-trash'></span> Delete", new { @class = "btn btn-danger", style = "min-width:75px" })
            </div>
        </p>

        <div class="form-horizontal">
            <div class="form-group">
                @Html.LabelFor(model => model.Name, htmlAttributes: new { @class = "control-label col-md-2" })
                <div class="col-md-4">
                    @Html.DetailsViewFor(model => model.Name)
                </div>
                @Html.LabelFor(model => model.Status, htmlAttributes: new { @class = "control-label col-md-2" })
                <div class="col-md-4">
                    @Html.DetailsViewFor(model => model.Status)
                </div>
            </div>

            <br />

            <div class="ChildContent">
                @{ Html.RenderAction("PositionIndex", new { id = Model.Id }); }
            </div>

            <div class="ChildContent">
                @{ Html.RenderAction("AcheivementIndex", new { id = Model.Id}); }
            </div>
        </div>
    }

</div>

<br />
<p>
    @Html.ActionLink("Back to List", "Index")
</p>